---
name: frontend-implementer
description: Implements frontend code (src/frontend/src/) for one view, given already-approved specs and red tests. Used by the Orchestrator's Phase B to run concurrently with backend-implementer.
tools: Read, Write, Edit, Bash, Glob, Grep
model: inherit
---

Read `lib/agents/frontend-implementer/frontend-implementer.md` in full and execute its
process to the letter, for the view named in your task prompt. Do not deviate from it, and
do not read or write anything under `src/backend/`. Report back exactly what its "Step 4 —
Confirm" section asks you to report.

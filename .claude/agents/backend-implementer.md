---
name: backend-implementer
description: Implements backend code (src/backend/src/) for one view, given already-approved specs and red tests. Used by the Orchestrator's Phase B to run concurrently with frontend-implementer.
tools: Read, Write, Edit, Bash, Glob, Grep
model: inherit
---

Read `lib/agents/backend-implementer/backend-implementer.md` in full and execute its
process to the letter, for the view named in your task prompt. Do not deviate from it, and
do not read or write anything under `src/frontend/`. Report back exactly what its "Step 4 —
Confirm" section asks you to report.

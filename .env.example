ANTHROPIC_API_KEY=sk-ant-...

# Pending — not configured yet. Required once view-designer/requirement-architect need to
# introspect or modify the live Postgres schema, and once implementer/reviewer need a real
# backend to run tests/coverage against.
DATABASE_URL=

# Used by ci-setup workflows and the reviewer agent's GitHub Issue tracking (gh CLI).
GITHUB_TOKEN=
SONAR_TOKEN=
CI_POSTGRES_PASSWORD=
